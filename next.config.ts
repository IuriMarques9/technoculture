import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["res.cloudinary.com"], // Substitua pelo domínio das suas imagens
  },
};

export default nextConfig;
